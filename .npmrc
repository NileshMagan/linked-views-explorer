# react-scripts@5.0.1 declares an optional peer of typescript@"^3.2.1 || ^4".
# That range predates TS 5 and CRA builds fine against it (verified by
# `npm run build`), but npm's strict peer resolution refuses the install.
# Set here rather than passed as a flag so a plain `npm install` works.
legacy-peer-deps=true
